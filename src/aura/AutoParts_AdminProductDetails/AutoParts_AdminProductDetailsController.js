/**
 * Created by BRITENET on 02.04.2019.
 */
({
    init: function(component, event, helper) {
        helper.doInit(component);
    },

    clickFileCheckbox: function(component, event, helper) {
        helper.doFileCheckbox(component, event);
    },

    clickAddMainImage: function(component, event, helper) {
        helper.doAddMainImage(component);
    },
})